# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
**BUG FIX & ENHANCEMENT:** The previous UI update incorrectly moved the Play/Start settings cog out of its colored zone banner. We must return the settings cog to the inside of the cyan "PLAY/START NOTE" banner, leaving *only* the Octave Knob in the row above it. 
Additionally, we need to adjust the default Play/Start octave offset dynamically based on the selected physical keyboard size: `-2` for 88-key, and `-1` for 49-key.

## 2. Technical Decisions & Dependencies
* **UI DOM Reversion:** The settings cog must be re-injected into the flex layout of the cyan banner. The Octave Knob will remain right-aligned in the control container above the banner.
* **State Mutation:** The `setInputKeyboardSize` action in the Zustand store must be updated to automatically adjust `playStartSettings.octaveOffset` whenever the keyboard size changes, ensuring the defaults map correctly to the physical limitations of the selected board.

## 3. Implementation Phases

### Phase 1: UI Reversion (Settings Cog) — ✅ Completed
* **Objective:** Put the Play/Start settings cog back inside the cyan banner.
* **Instructions:**
  1. In `src/components/KeySplitKeyboard.tsx`, locate the container `div` that holds the extracted controls (above the banners).
  2. Remove the `<Settings />` (or equivalent cog icon) from that container. The container should now *only* hold the Octave Knob.
  3. Re-insert the settings cog back into the JSX definition of the "PLAY/START NOTE" banner. Ensure it is positioned appropriately (likely using flexbox spacing to keep the text centered and the cog on the right edge of the banner).
* **TDD Checkpoint:**
  * **Test Case 1:** Render the component. Query the DOM to assert the settings cog icon is a child node of the cyan banner.
  * **Test Case 2:** Assert the Octave Knob remains a sibling element placed above the banner container.
  * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Update the JSX and styles. Execute tests. Once they PASS, set status to ✅ Completed."

### Phase 2: Dynamic Default Octave Offset — ✅ Completed
* **Objective:** Ensure the 49-key layout defaults to a `-1` octave offset.
* **Instructions:**
  1. In `src/store/useMidiStore.ts`, locate the `setInputKeyboardSize` action.
  2. Modify the action to update `playStartSettings.octaveOffset` concurrently.
     ```typescript
     setInputKeyboardSize: (size) =>
       set((state) => ({
         globalSettings: { ...state.globalSettings, inputKeyboardSize: size },
         playStartSettings: {
           ...state.playStartSettings,
           octaveOffset: size === 49 ? -1 : -2,
         },
       })),
     ```
  3. Ensure the initial default state for `playStartSettings.octaveOffset` remains `-2` (since the default keyboard size is 88).
* **TDD Checkpoint:**
  * **Test Case 1:** Call `setInputKeyboardSize(49)`. Assert `playStartSettings.octaveOffset` equals `-1`.
  * **Test Case 2:** Call `setInputKeyboardSize(88)`. Assert `playStartSettings.octaveOffset` equals `-2`.
  * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Update the store logic. Execute tests. Once they PASS, set status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Verify that returning the settings cog into the cyan banner did not break the dynamic width calculations of the header row.
* **Status Reconciliation:** Verify all sub-phases are ✅.
* **Final Human Sign-off:** **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to:
  1. Look at the Input card UI and verify the settings cog is back inside the cyan "PLAY/START NOTE" banner.
  2. Verify the Octave Knob is cleanly placed right-aligned above the banners.
  3. Toggle the Input Keyboard size to "49-Key" in the main settings.
  4. Verify the Octave Knob automatically changes its value to `-1`.
  5. Toggle back to "88-Key" and verify it switches back to `-2`.