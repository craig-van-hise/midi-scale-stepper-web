# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
**Project:** MIDI Scale Stepper MVP — **Job 1: Event Symmetry & Initialization**
**Objective:** The UI layout is currently locked and stable. However, the underlying event routing suffers from three critical mechanical failures: a "cold start" initialization bug, broken symmetry between physical MIDI inputs and the Scale Select UI, and broken symmetry between physical MIDI inputs and the Stepper UI. This job strictly focuses on unifying the event handlers so that physical MIDI inputs and mouse clicks execute the exact same application state changes. 

**CRITICAL MANDATE:** You are strictly forbidden from altering the CSS, DOM layout, key sizes, styling, or grid constraints of `ScaleKeySwitches12.tsx`, `ScaleStepperKeySwitches25.tsx`, or `KeySwitchContainer.tsx`. Do not touch the visual architecture. Focus entirely on React state, Zustand, and event routing.

## 2. Technical Decisions & Dependencies
* **State Unification:** All component `onClick` events and `onMIDIMessage` events must funnel into the same shared logic. 
* **Data Source Truth:** The physical MIDI keys in the Scale Select zone (C3-B3) must NOT attempt to mutate or overwrite the scale types. They must simply act as remote controls to select the active index of the existing Key Switches.

## 3. Implementation Phases

### Phase 1: The Initialization "Cold Start" Fix — ✅ Completed
* **Objective:** Ensure the Stepper engine is fully functional on page load without requiring the user to manually click a Scale Select switch first.
* **Instructions:**
    * In `useMidiStore.ts`, verify the initial state of `activeState`.
    * `scaleDecimalId` must initialize to the decimal value of C Major (`2741`), not `null`.
    * `rootNote` must initialize to `0` (C), not `null`.
    * `lastPlayedMidi` must initialize to `60` (Middle C), not `null`.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a fresh page load, When the Zustand store mounts, Assert `scaleDecimalId === 2741`, `rootNote === 0`, and `lastPlayedMidi === 60`]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply these default states. Once they pass validation, set status to ✅ Completed. Do not proceed until status is ✅."

### Phase 2: Scale Select Input Symmetry (C3-B3) — ✅ Completed
* **Objective:** Wire the physical MIDI input zone (C3-B3 / notes 48-59) to perfectly mirror a mouse click on the `ScaleKeySwitches12.tsx` UI.
* **Instructions:**
    * Locate the global MIDI Input Listener (where incoming MIDI notes are routed).
    * Intercept notes `48` through `59`.
    * Calculate the target index: `const switchIndex = incomingNote - 48;`
    * DO NOT use hardcoded test helper arrays. You must read the current `activeState.keySwitches` array from Zustand (or the equivalent array defining the 12 active switches).
    * Extract the `root` and `type` from that specific index. Calculate its decimal bitmask, calculate its root PC (0-11), and push these values to `useMidiStore` exactly as if the user had clicked that key on the screen.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a simulated Note On for MIDI 49 (C#3), When processed by the router, Assert Zustand `rootNote` updates to 1 and `scaleDecimalId` updates to the bitmask for Dorian (assuming default array)]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Implement this intercept and state push. Once passing, set status to ✅ Completed."

### Phase 3: Stepper UI Symmetry (Mouse Clicks) — ✅ Completed
* **Objective:** Ensure that clicking a key in `ScaleStepperKeySwitches25.tsx` executes the actual stepper calculation, rather than just changing a visual highlight.
* **Instructions:**
    * In `ScaleStepperKeySwitches25.tsx`, the `onClick` handler currently only does `setSelectedIndex(i)`. This is a dead end.
    * Create a placeholder stub for the stepper calculation (we will build the actual math in Job 2): 
        `const triggerStep = (stepOffset: number) => { console.log("Triggering step offset:", stepOffset); };`
    * Read the actual interval offset from `STEPPER_DATA_MAP[i].index`. Parse this string (e.g., `"-3"`, `"+2"`) into an integer.
    * Update the `onClick` handler on both the white and black keys to call `triggerStep(parsedOffset)` alongside `setSelectedIndex(i)`.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given a user click on the UI key labeled '+2', Assert `triggerStep` is invoked with the integer `2`]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Wire the `onClick` handlers to parse and pass the correct integer offset. Once passing, set status to ✅ Completed."
    * **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to verify in their browser console that clicking different Stepper UI keys correctly logs the expected positive or negative integer offsets."

## 4. Final Review & Validation
* **Technical Audit:** Verify that NO layout or CSS regressions have occurred in the KeySwitch container. Verify the infinite loop bug from previous sessions remains eradicated.
* **Status Reconciliation:** Verify all sub-phases are ✅ or ❌ before marking the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** AGENT INSTRUCTION: Stop and request the user to confirm Job 1 completion before requesting the PRP for Job 2.