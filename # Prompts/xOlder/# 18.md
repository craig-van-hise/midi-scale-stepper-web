# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
**Project:** MIDI Scale Stepper MVP — **Job 3: Output Filter Restoration**
**Objective:** The core Stepper engine is fully functional. This final job restores the UI and routing logic for the Output Keyboard constraints. The agent must refactor the Output UI to display correct musical notation on the range sliders, remove unused filter modes, and inject the deterministic filter algorithms (derived from the original `wrappingLogic.js`) into the final stage of the MIDI routing pipeline.

## 2. Technical Decisions & Dependencies
* **Data Formatting:** The range slider must dynamically convert raw MIDI integers into standard scientific pitch notation (e.g., `60` -> `C4`) for the UI thumbs.
* **Filter Algorithms:** The system will strictly implement `octave_wrap` and `smart_wrap` using the exact mathematical logic provided by the project's original source code.

## 3. Implementation Phases

### Phase 1: Output UI Refactor — ✅ Completed
* **Objective:** Clean up the `NoteRangeFilterKeyboard.jsx` file, convert it to TypeScript, and implement the requested UI constraints.
* **Instructions:**
    * Rename `NoteRangeFilterKeyboard.jsx` to `.tsx` (or update its types if already TS).
    * **Settings Constraints:** In the `MODE_DESCRIPTIONS` object and the mapped array of modes in the `<Modal>`, **DELETE** `block`, `wrap`, and `limit`. Leave strictly `octave_wrap` and `smart_wrap`. Ensure the default mode in Zustand initializes to `octave_wrap`.
    * **Slider Typography:** Locate the `<Thumb>` component used by the `RangeSlider`. Currently, it displays the raw `value` (e.g., `60`).
    * Implement a local helper: 
      ```typescript
      const getNoteName = (midi: number) => {
        const pcs = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'];
        return `${pcs[midi % 12]}${Math.floor(midi / 12) - 1}`;
      };
      ```
    * Update the `<Thumb>` render to display `getNoteName(value)` instead of the raw integer.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the Output Keyboard settings menu, When opened, Assert only 'Octave Wrap' and 'Smart Wrap' radio buttons exist in the DOM]
    * **Test Case 2:** [Given the RangeSlider minimum value is set to 60, Assert the UI thumb text strictly evaluates to 'C4']
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Refactor the UI and pass these tests. Once passing, set status to ✅ Completed."

### Phase 2: The Filter Engine & Routing — ✅ Completed
* **Objective:** Inject the mathematical filtering constraints into the final step of the MIDI routing pipeline before the note is fired.
* **Instructions:**
    * Create a new utility function `applyOutputFilter(midiNote: number, mode: string, minRange: number, maxRange: number): number | null`
    * Implement the exact logic from the original `wrappingLogic.js` source code:
    ```typescript
    export function applyOutputFilter(midiNote: number, mode: string, minRange: number, maxRange: number): number | null {
      let finalNote = midiNote;

      if (finalNote >= minRange && finalNote <= maxRange) {
        return finalNote;
      }

      if (mode === 'octave_wrap') {
        while (finalNote < minRange) finalNote += 12;
        while (finalNote > maxRange) finalNote -= 12;
        
        if (finalNote < minRange || finalNote > maxRange) return null; // shouldDrop
        return finalNote;
      }

      if (mode === 'smart_wrap') {
        const pc = ((finalNote % 12) + 12) % 12;

        if (finalNote > maxRange) {
          let wrapped = minRange - (minRange % 12) + pc;
          if (wrapped < minRange) wrapped += 12;
          if (wrapped <= maxRange) return wrapped;
          return null; // shouldDrop
        } else if (finalNote < minRange) {
          let wrapped = maxRange - (maxRange % 12) + pc;
          if (wrapped > maxRange) wrapped -= 12;
          if (wrapped >= minRange) return wrapped;
          return null; // shouldDrop
        }
      }

      return null;
    }
    ```
    * Locate your `executeScaleStep` function (or central MIDI Out routing function). Retrieve `filterMode` and `filterRange` from `useMidiStore`.
    * Wrap the final calculated output note through this filter before sending it to the output buffer:
      ```typescript
      const finalMidi = applyOutputFilter(rawOutputMidi, filterMode, filterRange[0], filterRange[1]);
      if (finalMidi !== null) {
          useMidiStore.getState().setActiveState({ ...activeState, lastPlayedMidi: finalMidi });
          sendMidiToOutput(finalMidi); 
      }
      ```
* **TDD Checkpoint:**
    * **Test Case 1:** [Given input note 74, range [60, 71], and mode 'octave_wrap', Assert `applyOutputFilter` evaluates strictly to 62]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Implement the algorithm and routing. Once passing, set status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Ensure the `Piano88Filter` keys visually highlight when notes are routed through them, confirming end-to-end event flow from the Stepper engine through the Output constraints.
* **Status Reconciliation:** Verify all sub-phases are ✅ or ❌ before marking the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** AGENT INSTRUCTION: Stop and present the final MVP. Ask the user to intentionally scale-step past the bounds of the output range to manually verify that Octave Wrap and Smart Wrap function identically to the TDD logic.