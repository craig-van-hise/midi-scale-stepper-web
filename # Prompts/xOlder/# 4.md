# 🚨 ADDENDUM & COURSE CORRECTION 4: RESTORE VISUALS AND FIX CARD WIDTH

**AGENT INSTRUCTION:** Stop. We have made a critical error in the previous step. You must immediately undo the removal of the shadow blockers, and you must fix the white background card so it actually stretches behind the keys instead of letting them overflow.

### 1. RESTORE the Shadow Blockers
* **Action:** I previously instructed you to delete the `<div id="shadow-blocker-strip">`. That was a mistake. You must RESTORE that exact `div` in BOTH `ScaleKeySwitches12.tsx` and `ScaleStepperKeySwitches25.tsx` so the black gap between the top info strip and the keys is visible again.

### 2. Stretch the White Parent Card
* **Action:** Look at `KeySwitchContainer.tsx`. The root `div` (the one with `bg-white rounded-2xl shadow-md`) currently uses `w-full`. Because the child keyboards are so wide, they are overflowing this white background.
* **Fix:** Change the root wrapper's Tailwind class from `w-full` to `w-max inline-block` (or `w-fit min-w-full`). This forces the white background card to physically stretch and wrap around the full width of the scaled keyboards.

**STATUS UPDATE:**
Execute this immediately. Do not change the key sizes or the 0.80 scale. Just put the black strips back and fix the white card background. Stop and ask for validation when complete.