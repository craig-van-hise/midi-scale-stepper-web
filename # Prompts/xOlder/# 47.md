# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
**CRITICAL INCIDENT RECOVERY:** The project is currently in a broken state due to an incomplete revert/undo of a botched feature implementation. The Vite development server is crashing with a module resolution error: 
`[plugin:vite:import-analysis] Failed to resolve import "./ScaleStepperKeySwitches25" from "src/components/KeySwitchContainer.tsx". Does the file exist?`

**Objective:** Restore the missing `ScaleStepperKeySwitches25.tsx` component and any associated state files from the last stable Git commit/origin, repair the broken imports, and return the application to a compiling, test-passing state.

## 2. Technical Decisions & Dependencies
* **Git Operations:** The agent must use `git` commands to recover the exact file from the last stable commit (HEAD or origin/main) rather than attempting to rewrite the file from scratch.
* **No Feature Work:** Do NOT attempt to implement the 24-key or Two-Octave stepper during this operation. This is strictly a rollback and stabilization mission.
* **Clean Cache:** Vite's dependency cache may need to be cleared if module resolution caching holds onto the error after the file is restored.

## 3. Implementation Phases

### Phase 1: File & Codebase Recovery — ✅ Completed
* **Objective:** Recover the missing files from version control and ensure `KeySwitchContainer` has the correct imports.
* **Instructions:**
  1. Check the local git status (`git status`). Identify if `src/components/ScaleStepperKeySwitches25.tsx` is listed as deleted.
  2. Use `git restore src/components/ScaleStepperKeySwitches25.tsx` (and its test file `src/components/ScaleStepperKeySwitches25.test.tsx` if it exists) to pull them back from HEAD.
  3. If the file is not in local HEAD, pull it from the remote repository (e.g., `git checkout origin/main -- src/components/ScaleStepperKeySwitches25.tsx`).
  4. Inspect `src/components/KeySwitchContainer.tsx` on line 21. Ensure it correctly imports `ScaleStepperKeySwitches25`.
  5. Inspect `src/store/useMidiStore.ts`. If the previous agent corrupted the state (by adding `stepperKeyMap` or deleting the `lastPlayedMidi` logic), run `git restore src/store/useMidiStore.ts` to revert it to the last stable state.
* **TDD Checkpoint:**
  * **Test Case 1:** Run `ls src/components/` and assert that `ScaleStepperKeySwitches25.tsx` exists.
  * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute the git recovery commands. Once the file is physically verified on disk, set status to ✅ Completed."

### Phase 2: Build & Test Stabilization — ✅ Completed
* **Objective:** Prove the application compiles and the test suite passes without module errors.
* **Instructions:**
  1. If Vite is still running, restart it. If necessary, run `npx vite --force` to clear the module resolution cache.
  2. Run the Vite build command (`npm run build` or `npx tsc --noEmit`) to verify there are no TypeScript or module resolution errors.
  3. Run the unit test suite (`npm run test run`).
* **TDD Checkpoint:**
  * **Test Case 1:** Assert that `npm run test` executes and all tests pass (specifically looking for `KeySwitchContainer` or `ScaleStepperKeySwitches25` tests).
  * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Run the build and test commands. Do not proceed until tests are 100% green. Once they PASS, set status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Verify that the project tree matches the `PROJECT_STATE.md` structure exactly and that no lingering `...24.tsx` files exist in the components directory. If they do, delete them.
* **Status Reconciliation:** Verify all sub-phases are ✅ before marking the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to:
  1. Start the dev server (`npm run dev`).
  2. Open the browser and confirm the UI renders the original `-7 to +7` layout without crashing.
  3. Provide the clean code for `ScaleStepperKeySwitches25.tsx` and `useMidiStore.ts` so the Architect can safely draft the *new* feature PRP.