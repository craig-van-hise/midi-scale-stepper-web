
# Product Requirements Prompt (PRP) — 🔵 Active

## 1. Project Context & Objectives
The MIDI Scale Stepper MVP is a pure browser-based WebApp that maps specific MIDI key splits to scale degrees, allowing musicians to additively step through musical scales via interval mapping. This phase focuses entirely on internal logical routing, state management, and visual validation via the Web MIDI API, bypassing audio synthesis.

## 2. Technical Decisions & Dependencies

**Architectural Mandates:**
* **Frontend Stack:** React, TypeScript, Tailwind CSS.
* **Backend/Environment:** Vite (Single Page Application). Hardware I/O handled strictly via the browser's native **Web MIDI API** (`Navigator.requestMIDIAccess`).
* **DevOps/Infra:** Static site deployment (e.g., Vercel, Netlify, or containerized via Docker/Nginx).
* **Typography & Icons:** Google Fonts, Lucide React (MIT Licensed).
* **State Management:** Zustand (Essential for managing fast, transient MIDI state changes without deep prop-drilling or React context rendering penalties).
* **Testing Frameworks:** Vitest (Native Vite integration) and React Testing Library for component validation.
* **Global Dependency Constraints:** Strictly utilize open-source libraries with MIT or permissive equivalents for commercial compliance.

**Data Models & Schemas:**

**1. `PCS_LUT.dat` (JSON/Dictionary Schema):**
```json
{
  "decimal_id": {
    "name": "Scale Name",
    "scale_intervals": ["1", "b2", "b3", "4", "5", "b6", "b7"],
    "pitch_classes": [0, 1, 3, 5, 7, 8, 10] 
  }
}

```

**2. Zustand Global Store Schema (`useMidiStore`):**

```typescript
interface MidiState {
  globalSettings: {
    midiInPort: string | null;
    power: boolean; // bypass toggle
    channelFilter: number | 'ALL';
    startOctave: number; // 0-7
    roundPreference: 'UP' | 'DOWN';
  };
  activeState: {
    rootNote: number | null; // e.g., 0 for C, 1 for C#
    scaleDecimalId: number | null; // currently selected scale from 12-key selector
    lastPlayedMidi: number | null; // used for voice-leading calculation
  };
  uiState: {
    activeKeys: number[]; // currently depressed keys across all splits
  }
}

```

**Core Algorithms:**

**Algorithm 1: MIDI Selection & Bitmask Parsing**
To derive the integer index for the `PCS_LUT` lookup based on incoming chord/scale MIDI data:

1. **Extract Pitches:** Given an array of active MIDI notes $N = [n_1, n_2, \dots n_x]$.
2. **Determine Bass:** $bass\_note = \min(N) \implies bass\_pc = bass\_note \pmod{12}$.
3. **Normalize to Bass:** For each note $n$ in $N$, find its relative pitch class: $normalized\_pc = (n \pmod{12} - bass\_pc + 12) \pmod{12}$.
4. **Bitmask Generation:** Create a 12-bit binary string where index $i$ is `1` if $i \in normalized\_pcs$, else `0`. (Index 0 is the left-most bit).
5. **Reflection & Decimal Conversion:** Reverse the 12-bit string. Convert the reversed binary string to a base-10 decimal. Use this decimal as the key for `PCS_LUT`.

**Algorithm 2: Out-of-Scale Handling (Rounding Algorithm)**
When a calculated step target $T$ lands on a pitch class $P_{target}$ not present in the active `PCS_LUT.pitch_classes` array:

1. **If `roundPreference === 'UP'`:** Iterate $P_{target} + 1 \pmod{12}$ until a matching pitch class in the scale array is found. Output that note. If wrapping past 11, increment the target octave by 1.
2. **If `roundPreference === 'DOWN'`:** Iterate $P_{target} - 1 \pmod{12}$ until a matching pitch class is found. Output that note. If wrapping below 0, decrement the target octave by 1.

**Algorithm 3: Scale Transition & Voice-Leading Fallback**
Triggered when the user changes the active scale while holding/sustaining a note:

1. Extract pitch class of `lastPlayedMidi`: $P_{last} = lastPlayedMidi \pmod{12}$.
2. Check if $P_{last} \in new\_scale.pitch\_classes$.
3. If true, $new\_output\_note = lastPlayedMidi$.
4. If false, execute **Algorithm 2** starting from $P_{last}$ using the new scale boundaries to snap to the nearest valid note.

---

## 3. Implementation Phases

### Phase 1: Web Application Boilerplate & Web MIDI Integration — ✅ Completed

* **Objective:** Establish the foundational Vite SPA, global type declarations, and hardware permissions.
* **Instructions:**
* Initialize a Vite + React + TypeScript web application.
* Configure Tailwind CSS and inject Google Fonts.
* Define the TypeScript interfaces mapped exactly to the `Zustand Global Store Schema` outlined above.
* Implement a `useWebMidi` custom React Hook that invokes `Navigator.requestMIDIAccess()`, requests browser permissions, and pipes standard MIDI event messages (Note On/Note Off) directly into the Zustand store's `uiState.activeKeys`.
* Parse `PCS_LUT.dat` into a static TypeScript map upon application load so it remains strictly in browser memory for zero-latency lookups.


* **TDD Checkpoint:** * Tests established and passing. Agent instructed to maintain status.

### Phase 2: Core Algorithmic Engine (Math & Routing) — ✅ Completed

* **Objective:** Implement the headless MIDI logic layer to process math independently from React renders.
* **Instructions:**
* Build `BitmaskCalculator.ts` strictly implementing **Algorithm 1**.
* Build `RoundingEngine.ts` implementing **Algorithm 2**.
* Build `ScaleTransitionEngine.ts` utilizing **Algorithm 3**.


* **TDD Checkpoint:**
* Mathematical unit tests established and passing. Agent instructed to maintain status.



### Phase 3: Global Header & Settings Infrastructure — ✅ Completed

* **Objective:** Build the global control UI elements prioritizing a light theme and white cards.
* **Instructions:**
* Construct the Title Bar utilizing Lucide React icons for controls.
* Wire the "Panic (!)" button to reset state.
* Build Settings Modal (Cog) to expose state mutations.
* Build Info Modal (i).


* **TDD Checkpoint:**
* UI state mutations established and passing. Agent instructed to maintain status.



### Phase 4: Input Keyboard Components & Key Splits — ✅ Completed

* **Objective:** Implement the top card `KeySplitKeyboard` by securely adapting the pre-existing source file without destroying its visual styling.
* **Instructions:**
* **CRITICAL FILE PATH MANDATE:** Read the existing component source code exactly from:
* `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/src/plugins/midi-transposer/components/KeySplitKeyboard.jsx`
* `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/src/plugins/midi-transposer/components/keyboardMap.js`


* **DO NOT** hallucinate or rewrite the structural DOM, CSS wrappers, top bars, or knobs. PRESERVE the existing UI fidelity.
* Refactor the file to TypeScript (`.tsx`) strictly to add type safety and adapt the internal rendering logic for our specific zones.
* Update the split zones to the following hardcoded ranges:
* **Root Select (Orange):** MIDI Notes 36-47 (C2-B2). Sets `MidiState.activeState.rootNote`.
* **Scale Select (Yellow):** MIDI Notes 48-59 (C3-B3).
* **Stepper (Blue):** MIDI Notes 60-84 (C4-C6).
* **Thru (Slate Grey):** Notes `< 36` and `> 84`. Passes raw MIDI data directly to Output view.


* Wire the key visuals to highlight via `uiState.activeKeys` matching.


* **TDD Checkpoint:**
* **Test Case 1:** [Given a simulated Note On for MIDI 36, When evaluated by KeySplitKeyboard, Assert `activeState.rootNote` is updated to `0`]
* **Test Case 2:** [Given a simulated Note On for MIDI 24, When evaluated, Assert raw data passes to Thru without altering root or scale states]
* **Mocking Requirements:** Mock `useMidiStore` to inject `activeKeys` and observe mutations.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Write and execute these tests. Ensure they fail initially. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to visually confirm that the existing knobs and top bars from the original component remain intact, and that the Orange, Yellow, Blue, and Slate visual zones map exactly to the new ranges."



### Phase 5: KeySwitch UI & Middle Card Layout — ✅ Completed

* **Objective:** Build the dual side-by-side keyboard container for specialized functionality states using existing components.
* **Instructions:**
* **CRITICAL FILE PATH MANDATE:** Read the components from:
* Scale Select: `/Users/vv2024/Documents/Repos - vv2024/MIDI/react-midi-components/src/components/keyboards/ScaleKeySwitches12.tsx`
* Stepper: `/Users/vv2024/Documents/Repos - vv2024/MIDI/react-midi-components/src/components/keyboards/ScaleStepperKeySwitches25.tsx`


* Construct a shared Flex/Grid layout Card container to hold both components side-by-side. They currently lack a container.
* **Scale Select component:** Make NO CHANGES to the component internals. Import it, render it on the left side of the card, and wire it to Zustand.
* **Stepper component:** Refactor the target file strictly to remove the dynamic interval text rendered above the index. Leave the index labels intact. Render it on the right side of the card.


* **TDD Checkpoint:**
* **Test Case 1:** [Given the shared card renders, Assert `ScaleKeySwitches12` and `ScaleStepperKeySwitches25` are both present in the DOM within the shared wrapper.]
* **Test Case 2:** [Given `ScaleStepperKeySwitches25` renders, Assert the interval text nodes are no longer present in the document, leaving only index values.]
* **Mocking Requirements:** React Testing Library for DOM querying.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Write and execute these tests. Ensure they fail initially. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to confirm the shared card correctly contains both components side-by-side."



### Phase 6: Notation View & Output Visualization — 🔘 Pending

* **Objective:** Finalize visual output validators using existing components.
* **Instructions:**
* **CRITICAL FILE PATH MANDATE:** Read the components from:
* Scale Notation: `/Users/vv2024/Documents/Repos - vv2024/MIDI/react-midi-components/src/components/notation/ScaleInspectorNotation.tsx`
* Output Keyboard: `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/src/plugins/midi-transposer/components/NoteRangeFilterKeyboard.jsx`


* **Scale Notation component:** Make NO CHANGES to the component internals. Import it and establish a `useEffect` dependency on `MidiState.activeState.lastPlayedMidi` to pipe state into it.
* **Output Keyboard component:** Refactor the file (convert to `.tsx`). Modify the range slider handles to display standard note names (e.g., C1, C8) instead of MIDI integer values. In the internal settings logic of this component, purge the 3 unused filter modes, leaving strictly "Octave Wrap" and "Smart Wrap" available.


* **TDD Checkpoint:**
* **Test Case 1:** [Given the Output Keyboard renders, Assert the slider label displays 'C4' (or equivalent note name) instead of '60'.]
* **Test Case 2:** [Given the settings menu of the Output Keyboard is accessed, Assert only 'Octave Wrap' and 'Smart Wrap' are available options.]
* **Mocking Requirements:** React Testing Library for DOM and text validation.
* **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Write and execute these tests. Ensure they fail initially. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅."
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to manually verify the sliders show correct text strings and the settings menu is constrained properly."



## 4. Final Review & Validation

* **Technical Audit:** Run `npm run typecheck` and `npm run test`. Ensure zero `any` types remain in refactored TypeScript files. Use React DevTools profiler instructions to verify Zustand does not cause unnecessary global re-renders on rapid MIDI input.
* **Status Reconciliation:** Verify ALL phases (1 through 6) are marked ✅ Completed. The top-level PRP status MUST NOT be set to ✅ Completed if any phase remains 🔘, 🔵, 🚨, or ❌.
* **Final Human Sign-off:** AGENT INSTRUCTION: Provide the command `npm run dev` and output the local port. Instruct the user to connect a physical MIDI keyboard (or use the UI) and confirm end-to-end functionality before marking this document strictly as ✅ Completed.

```

