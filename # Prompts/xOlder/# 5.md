# 🚨 ADDENDUM & COURSE CORRECTION 5: Key Width Reduction & Layout Shift

**AGENT INSTRUCTION:** Pause current trajectory. We must optimize our screen real estate to prevent clipping and eliminate wasted margins. Execute the following precise layout and math modifications immediately.

### 1. Reduce Stepper Keyboard Width by 50%
In `ScaleStepperKeySwitches25.tsx`, you must cut the horizontal width of all keys exactly in half, while leaving their height completely untouched so they align vertically with the left component.
* **`getWhiteStyle`:** Change `width` from `81px` to `40.5px`. (Leave height at `132px`).
* **`getBlackStyle`:** Change `width` from `49.5px` to `24.75px`. Change the `right` offset from `-24.75px` to `-12.375px` (this perfectly centers it on the new seam). (Leave height at `84px`).
* **Container & Strip Width:** There are 15 white keys. 15 * 40.5px = 607.5px. Update the `width` property in BOTH `containerStyle` and `#scale-stepper-strip` to `607.5px`.

### 2. Fix the Ghost Margin on the White Card
In `KeySwitchContainer.tsx`, the `w-max` property is calculating based on the pre-scaled DOM, leaving ~500px of empty space.
* **Action:** To force the card to hug the scaled children tightly, replace `w-max` on the root wrapper with `w-fit`. 
* **Action:** Ensure the internal flex container holding the scaled items has a strictly defined width to prevent the card from blowing out. Calculate the new total width: Left Keyboard (567px) + Gap (24px) + Right Keyboard (607.5px) = 1198.5px. Update the `width` of the scaled inner flex div to exactly `1199px`.

### 3. Reclaim Global Left Margin
Both the Input Card and the KeySwitch Card are being pushed too far to the right by a global layout wrapper.
* **Action:** Locate the top-level layout component rendering these cards (e.g., `App.tsx`, `MainLayout.tsx`, or the parent page wrapper). 
* **Action:** Identify excessive margin/padding classes (e.g., `pl-12`, `ml-8`, `container`, or `max-w-7xl mx-auto`). Strip these back. Apply standard, minimal responsive padding (e.g., `px-4 md:px-8`) and ensure the container uses `max-w-[1600px] mx-auto` or `w-full` with left-alignment to shift the entire UI gracefully to the left side of the viewport.

**STATUS UPDATE:**
Make these precise integer and CSS changes. Do not touch the vertical heights of the keys. When complete, stop and ask the user to visually confirm that the Stepper keys are 50% thinner, the white card tightly hugs the components, and the entire app layout has shifted to the left.