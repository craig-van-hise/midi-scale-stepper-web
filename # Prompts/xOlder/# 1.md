# 🚨 ADDENDUM & COURSE CORRECTION: Phase 5 Layout Failure

**AGENT INSTRUCTION:** Pause current trajectory. Your implementation of the Phase 5 layout has failed our UI constraints. You have introduced horizontal scrolling/clipping, mismatched component heights, and incorrectly nested card styles. 

Execute the following architectural fixes immediately to correct the DOM:

### 1. Destroy Nested Card Styling
The `ScaleKeySwitches12` and `ScaleStepperKeySwitches25` components are currently rendering as independent cards. 
* **Action:** Strip all background colors, borders, shadows, and padding from the root wrappers of both imported components. They must render completely flat and transparent.

### 2. Implement a Single Parent Card Wrapper
You must wrap both flattened components in **one** shared container.
* **Action:** Create a single parent `div` (or Card component) with our standard UI styling (e.g., `bg-white shadow-md rounded-lg p-4`).

### 3. Enforce Grid/Flex Sizing & Prevent Clipping
The right-side keyboard is currently overflowing the viewport and causing horizontal scrolling, and the heights are completely misaligned.
* **Action:** Apply a strict layout to the parent wrapper (e.g., `grid grid-cols-[minmax(0,1fr)_minmax(0,2fr)] gap-4` or a tightly constrained flexbox).
* **Action:** Force both child components to stretch to the exact same height (`h-full`, `items-stretch`).
* **Action:** Add `overflow-hidden` to the parent card. Ensure any SVG or DOM keys inside the keyboards use CSS to scale proportionally to their container (`w-full max-w-full`) so they shrink to fit rather than bleeding off the screen.

**STATUS UPDATE:**
Do not modify the master PRP document. Implement these CSS/DOM fixes immediately. Keep Phase 5 marked as `🔵 Active`. When you have applied the fixes, stop and ask the user to visually confirm that both keyboards render smoothly on a single card without clipping.