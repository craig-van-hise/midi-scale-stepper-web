# 🚨 ADDENDUM & COURSE CORRECTION 6: Header UI & Card Normalization

**AGENT INSTRUCTION:** Pause current trajectory. The KeySwitch container has hallucinated title text, incorrect typography, and bloated internal padding. We are going to perfectly align its styling and width to match the "Input" card above it.

Execute the following exact modifications in `KeySwitchContainer.tsx`:

### 1. Rebuild the Header to Match "Input"
You hallucinated the text "SCALE SELECTION & ADDITIVE STEP CONFIGURATION" and applied an uppercase, tracked-out font. That is incorrect.
* **Action:** Import `ChevronUp` (or `ChevronDown`) from `lucide-react`.
* **Action:** Replace the current `<h3>` header with a flex container that perfectly clones the Input card's header. 
* **Implementation:** `<div className="flex items-center gap-2 mb-4 text-gray-600 font-semibold text-lg cursor-pointer">`
  `  <ChevronUp className="w-5 h-5" />`
  `  <span>Key switches</span>`
  `</div>`
* *(Note: Cross-reference the exact Tailwind text-color and font-size classes used in the top Input card header and use them here).*

### 2. Match Card Width to Input Card
The KeySwitch card is awkwardly sized. It must be exactly the same width as the Input card.
* **Action:** Inspect the parent wrapper of the top Input keyboard. Apply those **exact same width constraints** (e.g., `w-full max-w-[X]` or identical grid column spanning) to the root `div` of `KeySwitchContainer`.
* **Action:** Ensure the internal div holding the scaled keyboards has `justify-center` or `mx-auto` so that the two keyboards sit perfectly centered inside this newly widened card, leaving equal negative space on the left and right.

### 3. Tighten Internal Margins & Padding
The card has massive empty spaces on the bottom, top, and right.
* **Action:** On the root `div` of `KeySwitchContainer`, reduce the padding (e.g., change `p-6` to `p-4` or `px-6 py-4` to match the Input card's padding).
* **Action:** Locate the inner wrapper `<div className="w-full flex justify-center overflow-hidden h-[135px] mb-[45px]">`. Remove that excessive `mb-[45px]` margin immediately. Change the `h-[135px]` to `h-auto` or a tighter value so it doesn't leave empty space below the 0.80 scaled keys.

**STATUS UPDATE:**
Execute these precise DOM/CSS adjustments. Do not alter the physical key sizes or the 0.80 scale we established in previous steps. Once complete, stop and ask the user to visually confirm that the header now reads "Key switches" with a chevron, and that the card's width and padding flawlessly mirror the Input card.