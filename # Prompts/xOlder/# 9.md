# 🚨 ADDENDUM & COURSE CORRECTION 9: PCS_LUT Integration & Two-Way Binding Fix

**AGENT INSTRUCTION:** Pause current trajectory. The Notation component is rendering a corrupted "Unknown Scale" with flat ledger lines (C, Cx, C). This is happening because `entry` is resolving to `null`, forcing all intervals to fallback to `'1'` (Unison). 

Furthermore, you have localized the scale names in `ScaleKeySwitches12` and used a `.test-helper` file instead of the actual `PCS_LUT`, completely breaking the two-way editability the user requires.

Execute the following architectural fixes immediately:

### 1. Fix the LUT Lookup & Math in Notation
In `ScaleInspectorNotation.tsx`, your inline bitmask math `Math.pow(2, pc)` violates Algorithm 1 (which requires bit reflection).
* **Action:** Import `calculateBitmaskDecimal` from the central math utility (e.g., `BitmaskCalculator.ts`).
* **Action:** Replace the inline `reduce` math with: `const bitmask = calculateBitmaskDecimal(pcs);`
* **Action:** Fix the `lut` lookup. If the global parsed `PCS_LUT` was implemented as a TypeScript `Map` in Phase 1, `lut[bitmask]` will return `undefined`. You MUST check the data structure and use `lut.get(bitmask)` if it is a Map, or `lut[bitmask.toString()]` if it is an Object.

### 2. Fix the Sync Race Condition (Notation -> Store)
In my previous addendum, I mistakenly instructed you to use a `useEffect` to sync `scaleData.bitmask` to `useMidiStore`. This causes an infinite loop/override on mount.
* **Action:** DELETE the `useEffect` that listens to `scaleData?.bitmask`.
* **Action:** To push edits to the global store, you must ONLY call `setScaleDecimalId(newBitmask)` manually *inside* the interaction functions (`mutateSelected`, `deleteSelected`, `insertNoteMidway`) immediately after calculating the new proposed pitches and determining their valid bitmask.

### 3. Establish Two-Way Sync (Store -> KeySwitches)
When the user edits a scale in the Notation component, it updates `scaleDecimalId` in Zustand. The yellow active key in `ScaleKeySwitches12` must update its text to reflect this new scale.
* **Action:** Remove the `SCALES_ROSTER` test helper import in `ScaleKeySwitches12.tsx`. You must use the actual `PCS_LUT`.
* **Action:** Add a `useEffect` inside `ScaleKeySwitches12.tsx` that listens to `useMidiStore((state) => state.activeState.scaleDecimalId)`. 
* **Action:** Inside that `useEffect`, if `scaleDecimalId` changes, look it up in the `PCS_LUT`. If found, extract the `scale_type` (e.g., "Mixolydian"). Update the local `scales` array at the `selectedIndex` to dynamically display this new type on the key.

**STATUS UPDATE:**
Execute these precise logic fixes. The goal is that the user can select a Key Switch, see it populate perfectly in the Notation staff, edit the notes chromatically, and watch the Key Switch text magically update to the new scale name. Stop and ask for validation when complete.