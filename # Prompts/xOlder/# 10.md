
# 🚨 ADDENDUM & COURSE CORRECTION 10: Fatal Crash & Math Normalization Fixes

**AGENT INSTRUCTION:** Pause current trajectory. Your previous code introduced fatal TypeErrors (causing white screen crashes) and mathematical corruption (causing the snap-back feedback loop). 

Execute the following surgical fixes immediately:

### 1. Fix the Fatal Crash (`pitch_classes` vs `pitch_class_set`)
In `ScaleInspectorNotation.tsx`, inside the `useEffect` that syncs with the store active scale:
* **Action:** You are attempting to map over `entry.pitch_class_set`. This property does NOT exist in the schema. Change it to `entry.pitch_classes`.

### 2. Fix the Zero-Normalization Math (Snap-Back Fix)
In `ScaleInspectorNotation.tsx`, your math for `newPitches` double-subtracts the root note, corrupting the scale and causing an infinite update loop. Because the `PCS_LUT` is already zero-normalized, you simply add the `rootMidi` to the pitch class.
* **Action:** Replace the entire `newPitches` map function with this exact code:
```typescript
const newPitches = entry.pitch_classes.map((pc: number) => {
  return rootMidi + pc;
}).sort((a: number, b: number) => a - b);

```

### 3. Clean up the LUT Lookups

In both `ScaleInspectorNotation.tsx` and `ScaleKeySwitches12.tsx`, you wrote a messy fallback: `(lut as any).get?.(id) || (lut as any)[id]`.

* **Action:** `lutRegistry.ts` explicitly exports an array `(PCS_Entry | null)[]`. Therefore, standard array indexing is completely safe. Replace those messy lines with simply: `const entry = lut[scaleDecimalId];` (or `lut[bitmask]`).

**STATUS UPDATE:**
Execute these precise line edits. Do not rewrite the rest of the components, and preserve `entry.scale_type`. Once complete, stop and ask the user to validate: click "C# Dorian" on the Key Switch, verify it does not crash, and verify that pressing ArrowUp on a note accurately mutates the scale without snapping back.

```
