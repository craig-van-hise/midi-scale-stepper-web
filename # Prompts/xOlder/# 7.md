

# 🚨 ADDENDUM & COURSE CORRECTION 7: Phase 6 Vertical Optimization & Notation Font

**AGENT INSTRUCTION:** Pause current trajectory. The UI is suffering from vertical bloat, causing the Output keyboard to fall off the screen. Furthermore, the Notation card is misaligned (pushed to the right), and the music notation symbols are broken due to a missing font. 

Execute the following architectural and CSS fixes immediately:

### 1. Bravura Font Integration
The notation component relies on the SMuFL standard font `Bravura`.
* **Action:** Copy the `Bravura.woff2` file from `/Users/vv2024/Documents/Repos - vv2024/MIDI/WebApps/midi-web-apps-portal/public/fonts` into this project's `/public/fonts/` directory.
* **Action:** Inject the following `@font-face` declaration into your global CSS file (e.g., `index.css` or `App.css`) at the very top:
```css
@font-face {
  font-family: 'Bravura';
  src: url('/fonts/Bravura.woff2') format('woff2');
  font-weight: normal;
  font-style: normal;
  font-display: block;
}

```

### 2. Global Card Alignment & Width Normalization

The Notation card is pushed to the right. All four cards (Input, Key Switches, Notation, Output) must be perfectly left-aligned and share the exact same width.

* **Action:** Ensure the parent wrapper rendering these four cards uses a uniform flex/grid column layout with left-alignment (e.g., `flex flex-col items-start w-full max-w-[...]`).
* **Action:** Apply the exact same width constraint to the root wrapper of the `ScaleInspectorNotation` card and the Output keyboard card so they physically match the Input and Key Switches cards.
* **Note:** For the Notation card, allow the internal content (the staff) to have generous horizontal margins (`mx-auto` or high `px`), but the *white background card itself* must stretch to match the others.

### 3. Global Vertical Compression (Gaps)

* **Action:** In the main layout component holding the four cards, reduce the vertical gap between them. If you are using `gap-8` or `space-y-8`, reduce it to `gap-3` or `space-y-3`.

### 4. Notation Card Vertical Compression

The Notation card has excessive empty space.

* **Action:** Inside `ScaleInspectorNotation.tsx`, significantly reduce the top padding, the bottom padding, and the margin below the "C Major" title.
* **Action:** Reduce the overall bounding height of the SVG staff container so it takes up minimal vertical space.

### 5. Key Switch Height Reduction (~10%)

To save further vertical space, slightly shorten the physical keys in both `ScaleKeySwitches12.tsx` and `ScaleStepperKeySwitches25.tsx`. DO NOT change their widths.

* **Action:** In `getWhiteStyle`, change `height` from `132px` to `118px`.
* **Action:** In `getBlackStyle`, change `height` from `84px` to `75px`.

**STATUS UPDATE:**
Make these precise adjustments. Ensure all cards are flush left, uniform in width, and the vertical footprint is minimized. Once complete, stop and ask the user to visually confirm that the Bravura font renders correctly and all four cards are visible on a standard screen.

```
