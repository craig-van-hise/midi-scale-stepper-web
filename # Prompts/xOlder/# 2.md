# 🚨 ADDENDUM & COURSE CORRECTION 2: Phase 5 Layout Distortion

**AGENT INSTRUCTION:** Pause current trajectory. Your latest render has critically failed our visual constraints. You have distorted the native aspect ratios of the keys, altered the vertical spacing between the info strips and the keys, and you are still rendering dark backgrounds behind the individual components.

You are over-engineering the CSS. Both `ScaleKeySwitches12` and `ScaleStepperKeySwitches25` are built from the same core keyboard specifications. They natively have the same height and internal proportions. 

Execute the following fixes immediately:

### 1. Revert Internal Distortions
* **Action:** REMOVE any custom heights, flex-grow (`flex-1`), or internal grid restrictions you applied to the keys, info strips, or gaps. Let the components render at their native, original internal dimensions. Do not touch the space between the top info strip and the keys.

### 2. Strip the Dark Backgrounds (Card-in-Card Fix)
* **Action:** Locate the root `div` wrappers inside `ScaleKeySwitches12.tsx` and `ScaleStepperKeySwitches25.tsx`. REMOVE any classes applying dark backgrounds (e.g., `bg-neutral-900`, `bg-black`, `bg-[#...]', `dark:...`). They must have transparent backgrounds so they sit flatly on the unified white parent card.

### 3. Uniform Proportional Scaling
* **Action:** To make them fit horizontally without clipping, DO NOT squish their widths. Instead, use a Flexbox container with a uniform scale modifier, or rely on native responsive width scaling that preserves the aspect ratio. 
* **Implementation:** Wrap both transparent components in a simple flex row: `<div className="flex flex-row items-start justify-center gap-6 w-full">`. 
* **If they still overflow:** Apply a uniform CSS `transform: scale(0.85)` (or similar percentage) with `transform-origin: top center` to the Flex container or the child wrappers, so both keyboards scale down uniformly by the exact same percentage. 

**STATUS UPDATE:**
Implement these fixes immediately. Do not alter any core logic. When you have applied these CSS fixes, stop and ask the user to visually confirm the distortion is gone and the dark backgrounds are removed.