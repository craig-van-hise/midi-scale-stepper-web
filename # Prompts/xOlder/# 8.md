# 🚨 ADDENDUM & COURSE CORRECTION 8: Restore Dropped Interactivity in Notation

**AGENT INSTRUCTION:** Pause current trajectory. During the refactor of `ScaleInspectorNotation.tsx`, you improperly truncated approximately 100 lines of critical interaction logic from the original source file. The user can currently only select notes via mouse clicks. All keyboard navigation, chromatic mutation, and structural scale edits (adding/deleting notes) are missing.

You must immediately retrieve the original script logic and inject it back into `ScaleInspectorNotation.tsx`. Execute the following surgical restorations:

### 1. Restore the Core Mutation Handlers
Extract the following exact functions from the original source code (`ScaleInspectorNotation-1.tsx`) and inject them into the current component (below the existing `handleNoteClick` function):
* **`mutateSelected(delta: number)`**: Restores the Octave Wrap Algorithm and strict monotonicity checks.
* **`deleteSelected()`**: Restores the ability to remove a note (maintaining a minimum of 2 notes).
* **`clearSelection()`**: Resets the selection state.
* **`insertNoteMidway(event)`**: Restores the transactional validation for double-clicking to add notes between existing indices based on DOM geometry (`getBoundingClientRect`).

### 2. Restore the Global Keyboard Listener
Extract the global `useEffect` from the original file that listens for `keydown` events. 
* **Action:** Inject this hook so that `ArrowUp`/`ArrowDown` triggers `mutateSelected`, `ArrowLeft`/`ArrowRight` changes the `selectedIndices`, `Escape` triggers `clearSelection`, and `Delete`/`Backspace` triggers `deleteSelected`.

### 3. Re-Bind the DOM Events
The JSX wrappers were stripped of their event handlers. Restore them:
* **Action:** Add `onClick={clearSelection}` to the root container (`<div style={containerStyle} className="scale-inspector-container" ...>`).
* **Action:** Add `onDoubleClick={insertNoteMidway}` to the inner Notes Container (`<div ref={containerRef} ...>`).

### 4. Wire Two-Way State Sync (Architectural Mandate)
Currently, `pitches` are only set when the Zustand store updates (`useEffect` on `scaleDecimalId`). If the user modifies pitches locally via these restored functions, the rest of the application must know.
* **Action:** Import `setScaleDecimalId` from `useMidiStore`.
* **Action:** In the `useMemo` block that calculates `scaleData`, you are already deriving the `bitmask` from the local `pitches` array. Add a `useEffect` that listens to `scaleData.bitmask`. Whenever the `bitmask` changes as a result of user interaction (up/down arrows, delete, insert), push that new decimal to the global store: `setScaleDecimalId(scaleData.bitmask)`.

**STATUS UPDATE:**
Do not rewrite the visual CSS or standard layout logic we just fixed. Strictly restore these interactive functions from the original source. Once complete, stop and ask the user to validate that they can select a note, press Up/Down to change its pitch, and press Delete to remove it.