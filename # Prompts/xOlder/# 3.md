# 🚨 ADDENDUM & COURSE CORRECTION 3: Hardcoded Dimensional Normalization

**AGENT INSTRUCTION:** Pause current trajectory. The reason the keyboards look mismatched is that their internal, hardcoded CSS pixel dimensions are entirely different. We must normalize `ScaleStepperKeySwitches25` to perfectly match the key sizes of `ScaleKeySwitches12`, and then allow the parent container to expand to the right without arbitrary cropping.

Execute the following exact code changes immediately:

### 1. Normalize Keys in `ScaleStepperKeySwitches25.tsx`
You must manually update the hardcoded pixel values in this file to perfectly match the 12-key component.
* **`getWhiteStyle`:** Change `width` to `81px` and `height` to `132px`.
* **`getBlackStyle`:** Change `width` to `49.5px`, `height` to `84px`, and `right` to `-24.75px`.
* **Top Strip & Container:** The stepper has 15 white keys. 15 * 81px = 1215px. Update BOTH the `containerStyle` width and the `#scale-stepper-strip` width to `1215px`.

### 2. Remove Parent Strangling in `KeySwitchContainer.tsx`
The flex wrapper is artificially choking the layout with hardcoded pixel bounds.
* **Action:** In the div holding the `transform: scale(...)`, REMOVE `width: '1400px'`. Replace it with `width: 'max-content'` or remove the width property entirely so it naturally hugs the inner 1800px+ width.
* **Action:** REMOVE the hardcoded `h-[135px]` from the parent `div`. Allow the container to dynamically size to its children.
* **Scale Increase:** To achieve the user's requested ~10% size bump overall, change the `transform: 'scale(0.72)'` to `transform: 'scale(0.80)'`. Ensure `transformOrigin: 'top left'` (or center, depending on your flex alignment) so it scales cleanly within the bounding card.

### 3. Strip the Dark Artifacts
* **Action:** In both component files, locate the `<div id="shadow-blocker-strip" ... backgroundColor: '#1a1a1a' ... />` and **DELETE IT**. It is injecting a dark visual artifact between the strips and the keys that we do not want.

**STATUS UPDATE:**
Make these precise integer changes. Do not try to solve this with CSS flex-grow. Use the hard math above. Once implemented, stop and ask the user to confirm that the keys are now 100% identical in size and the layout successfully extends to the right.