# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
This is a comprehensive UI correction for the `ScaleChangeSettingsModal`. A previous prompt resulted in two errors: 
1. The trigger cog was incorrectly placed in the parent `KeySwitchContainer` rather than the `ScaleKeySwitches12` info strip. 
2. The modal styling was a hybrid, failing to match the target "Output Settings" modal (which uses radio buttons and lacks a top-right "X" close button). 
The objective is to fix the component hierarchy, relocate the cog, and completely rewrite the modal's internal UI to match the provided design constraints.

## 2. Technical Decisions & Dependencies
* **DOM Relocation:** The cog must be absolutely positioned within the `#scale-key-switches-strip` relative container in `ScaleKeySwitches12.tsx`.
* **Styling Strictness:** The modal UI must use native radio buttons, an uppercase tracking-wide subtitle, and a black "Done" button. The top-right "X" button must be removed to match the reference design.

## 3. Implementation Phases

### Phase 1: Excision from KeySwitchContainer — ✅ Completed
* **Objective:** Remove the misplaced modal state and trigger from the parent container.
* **Instructions:**
    1.  Open `src/components/KeySwitchContainer.tsx`.
    2.  Remove the `Settings` icon import and the `ScaleChangeSettingsModal` import.
    3.  Delete the local state hook: `const [isSettingsOpen, setIsSettingsOpen] = useState(false);`.
    4.  Remove the `<button>` containing the Settings cog from the header block.
    5.  Remove the `<ScaleChangeSettingsModal ... />` component from the bottom of the JSX return.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given `KeySwitchContainer` renders, Assert the Settings cog and modal are no longer in the DOM.]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Execute removals. Once PASSING, set status to ✅ Completed."

### Phase 2: Relocation to ScaleKeySwitches12 — ✅ Completed
* **Objective:** Inject the trigger exactly on the right side of the Scale info strip.
* **Instructions:**
    1.  Open `src/components/ScaleKeySwitches12.tsx`.
    2.  Import `Settings` from `lucide-react` and import `ScaleChangeSettingsModal`.
    3.  Add local state: `const [isSettingsOpen, setIsSettingsOpen] = useState(false);`
    4.  Locate the `<div id="scale-key-switches-strip" ...>` element.
    5.  Inside this strip, exactly after the `top-strip-type` span, inject the trigger button:
        ```tsx
        <button
          onClick={(e) => { e.stopPropagation(); setIsSettingsOpen(true); }}
          style={{ position: 'absolute', right: '12px' }}
          className="p-1 text-gray-400 hover:text-gray-800 transition-colors cursor-pointer"
          title="Scale Change Settings"
        >
          <Settings className="w-4 h-4" />
        </button>
        ```
    6.  At the very bottom of the component's return statement (just before the final closing `</div>`), render `<ScaleChangeSettingsModal isOpen={isSettingsOpen} onClose={() => setIsSettingsOpen(false)} />`.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given `ScaleKeySwitches12` renders, Assert the Settings cog is present inside the `#scale-key-switches-strip` element and clicking it opens the modal.]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Apply changes. Once PASSING, set status to ✅ Completed."

### Phase 3: Restyle ScaleChangeSettingsModal — ✅ Completed
* **Objective:** Rewrite the modal UI to perfectly match the Output Settings radio-button style.
* **Instructions:**
    1.  Open `src/components/ScaleChangeSettingsModal.tsx`.
    2.  Remove the `X` icon import from `lucide-react` and delete the `<button>` containing the `X` entirely.
    3.  Ensure the title is `Scale Change Settings` using the classes: `text-xl font-bold font-display text-gray-800 mb-6`.
    4.  Replace the entire flex-button block with the following Radio Button UI structure to match the Output Settings styling:
        ```tsx
        <div className="space-y-4 mb-8">
          <h3 className="text-xs font-bold text-gray-500 uppercase tracking-wider mb-4">
            Behavior Mode
          </h3>
          
          <label className="flex items-center gap-3 cursor-pointer">
            <input 
              type="radio" 
              name="scaleChangeMode"
              value="follow-root"
              checked={scaleChangeMode === 'follow-root'}
              onChange={() => setScaleChangeMode('follow-root')}
              className="w-4 h-4 text-blue-600 border-gray-300 focus:ring-blue-500 cursor-pointer"
            />
            <span className="text-sm font-semibold text-gray-800">Follow Root</span>
          </label>

          <label className="flex items-center gap-3 cursor-pointer">
            <input 
              type="radio" 
              name="scaleChangeMode"
              value="voice-leading"
              checked={scaleChangeMode === 'voice-leading'}
              onChange={() => setScaleChangeMode('voice-leading')}
              className="w-4 h-4 text-blue-600 border-gray-300 focus:ring-blue-500 cursor-pointer"
            />
            <span className="text-sm font-semibold text-gray-800">Voice-Leading</span>
          </label>
        </div>
        ```
    5.  Ensure the "Done" button is styled as: `w-full py-2.5 bg-gray-950 hover:bg-gray-900 text-white font-semibold rounded-lg text-sm transition-colors cursor-pointer`.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given the modal is open, Assert the X button does not exist.]
    * **Test Case 2:** [Given the modal is open, Assert native radio inputs are present and clicking "Voice-Leading" updates the store.]
    * **AGENT INSTRUCTION:** "Set Phase status to 🔵 Active. Rewrite the UI and test. Once PASSING, set status to ✅ Completed."

## 4. Final Review & Validation
* **Technical Audit:** Verify the modal relies on standard HTML radio buttons and the `useMidiStore` properly syncs the state. Ensure the cog fits cleanly inside the white info strip without breaking the centered text.
* **Status Reconciliation:** Verify Phases 1, 2, and 3 are marked ✅ Completed. If so, mark the top-level PRP status as ✅ Completed.
* **Final Human Sign-off:** "STOP. Present the completed work to the user. **HUMAN VALIDATION REQUIRED**: Ask the user to: 1) Verify the cog is on the far right of the white 12-key switch info strip. 2) Click the cog and verify the modal perfectly matches the 'Output Settings' radio-button styling with no X button."